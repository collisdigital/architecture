[project]
name = "architecture-decision-records"
version = "0.1.0"
description = "MKDocs Material Setup for ADR site"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mkdocs-material>=9.6.9",
]
